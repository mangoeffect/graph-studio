build/
dist/
build_win
build_wasm/
build_android/
build_ios/
# 移动端打包脚本的 per-slice / per-ABI 构建目录（build_ios.sh / build_android.py）
build_ios_device/
build_ios_sim/
build_android_*/
build_coretest
submodules/*
/AGENTS.md
/.zcode
/.mimosa
.mimosa/
/.trae
.DS_Store
app/graph_studio/build/
app/graph_studio/build_wasm/profiler_trace.json
app/graph_studio/third_party/
# 根 third_party：miniz / quickjs 是提交入库的 vendored 源码（miniz 编入
# .tgp 工程包 zip 读写；quickjs 编入核心库 JS 引擎，见 src/js/），其余未来
# fetch 产物不入库。必须用 /* 通配而非 "third_party/"——整目录排除后 git
# 不再下钻，"!third_party/miniz/" 永不生效（app 侧旧写法即踩此坑）。
third_party/*
!third_party/miniz/
!third_party/quickjs/

# Python (cross-platform scripts under scripts/)
__pycache__/
*.pyc

# Website (docs/ 是 Hugo 站点源码；以下为构建期产物)
docs/public/
docs/resources/_gen/
docs/.hugo_build.lock
docs/data/releases.json
/profile_trace.json

# 测试模型/测试图（scripts/download_*_models.py 下载，不入库：face/matting/
# mediapipe 三套——mediapipe 集供 face/matting 的 mp 后端与共享测试图）
tests/models/
